@extends('layouts.site')
@section('meta_title', 'About — '.($siteSettings['site_name'] ?? config('app.name')))
@section('content')
<section class="page-hero section-pad"><div class="container narrow-wide"><span class="eyebrow">About us</span><h1>Built for companies that value clarity over noise.</h1><p>{{ $siteSettings['about_text'] ?? 'We are a multidisciplinary company focused on useful strategy, thoughtful design and dependable delivery.' }}</p></div></section>
<section class="section-pad soft-section"><div class="container split-feature"><div><span class="eyebrow">Our point of view</span><h2>Good work reduces complexity.</h2></div><div class="prose-large"><p>We believe corporate communication should make decisions easier. That means fewer empty claims, stronger evidence, cleaner systems and a digital experience that respects people’s time.</p><p>Our model keeps senior people close to the work from the first conversation through launch and improvement.</p></div></div></section>
<section class="section-pad"><div class="container"><div class="section-heading"><div><span class="eyebrow">Leadership</span><h2>The people responsible for the work.</h2></div></div><div class="team-grid">@forelse($team as $member)<article class="team-card"><div class="team-photo">@if($member->image)<img src="{{ asset('storage/'.$member->image) }}" alt="{{ $member->name }}" loading="lazy" width="520" height="620">@else<span>{{ strtoupper(substr($member->name,0,1)) }}</span>@endif</div><h3>{{ $member->name }}</h3><span>{{ $member->role }}</span><p>{{ $member->bio }}</p></article>@empty<div class="empty-state">Team profiles can be managed from the admin panel.</div>@endforelse</div></div></section>
<section class="cta-section"><div class="container cta-inner"><div><span class="eyebrow">Work with us</span><h2>Bring us the complicated version.</h2></div><a class="btn btn-dark" href="{{ route('contact') }}">Talk to the team <span>↗</span></a></div></section>
@endsection
