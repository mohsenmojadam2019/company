@extends('layouts.admin')
@section('title','جزئیات پیام')
@section('content')
<div class="admin-intro"><div><h2>{{ $message->subject ?: 'درخواست تماس' }}</h2><p>ارسال‌شده توسط {{ $message->name }} در {{ $message->created_at->format('Y/m/d H:i') }}</p></div><a class="secondary-btn" href="{{ route('admin.messages.index') }}">بازگشت</a></div><div class="admin-grid"><section class="admin-card"><div class="prose">{!! nl2br(e($message->message)) !!}</div></section><aside class="admin-card project-facts"><div><small>نام</small><strong>{{ $message->name }}</strong></div><div><small>ایمیل</small><strong>{{ $message->email }}</strong></div>@if($message->phone)<div><small>تلفن</small><strong>{{ $message->phone }}</strong></div>@endif @if($message->company)<div><small>شرکت</small><strong>{{ $message->company }}</strong></div>@endif<form method="POST" action="{{ route('admin.messages.destroy',$message) }}" onsubmit="return confirm('پیام حذف شود؟')">@csrf @method('DELETE')<button class="secondary-btn full-btn" type="submit">حذف پیام</button></form></aside></div>
@endsection
