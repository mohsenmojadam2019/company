@extends('layouts.site')
@section('meta_title', 'Contact — '.($siteSettings['site_name'] ?? config('app.name')))
@section('content')
<section class="page-hero section-pad"><div class="container narrow-wide"><span class="eyebrow">Contact</span><h1>Tell us what you are trying to change.</h1><p>Share the useful version: business goal, current problem, timing and what a good outcome looks like.</p></div></section>
<section class="section-pad soft-section"><div class="container contact-grid"><aside class="contact-aside"><span class="eyebrow">Direct</span>@if($siteSettings['email'] ?? null)<a href="mailto:{{ $siteSettings['email'] }}">{{ $siteSettings['email'] }}</a>@endif @if($siteSettings['phone'] ?? null)<a href="tel:{{ preg_replace('/\s+/', '', $siteSettings['phone']) }}">{{ $siteSettings['phone'] }}</a>@endif @if($siteSettings['address'] ?? null)<p>{{ $siteSettings['address'] }}</p>@endif</aside><div class="contact-form-wrap">@if(session('success'))<div class="alert success">{{ session('success') }}</div>@endif @if($errors->any())<div class="alert error">Please review the highlighted fields.</div>@endif<form class="contact-form" method="POST" action="{{ route('contact.store') }}">@csrf <div class="honeypot" aria-hidden="true"><label>Website<input type="text" name="website" tabindex="-1" autocomplete="off"></label></div><div class="form-row"><label>Name<input name="name" value="{{ old('name') }}" required autocomplete="name"></label><label>Email<input type="email" name="email" value="{{ old('email') }}" required autocomplete="email"></label></div><div class="form-row"><label>Phone<input name="phone" value="{{ old('phone') }}" autocomplete="tel"></label><label>Company<input name="company" value="{{ old('company') }}" autocomplete="organization"></label></div><label>Subject<input name="subject" value="{{ old('subject') }}"></label><label>What do you need help with?<textarea name="message" rows="7" required>{{ old('message') }}</textarea></label><button class="btn btn-dark" type="submit">Send enquiry ↗</button></form></div></div></section>
@endsection
