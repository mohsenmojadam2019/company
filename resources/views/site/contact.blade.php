@extends('layouts.site')
@section('meta_title','تماس با ما — '.($siteSettings['site_name'] ?? config('app.name')))
@section('meta_description','برای مشاوره ساخت برج، ویلا یا پروژه ساختمانی با آریا سازه در ارتباط باشید.')
@section('content')
<section class="contact-hero"><div class="lux-container"><span class="gold-kicker">شروع یک پروژه جدید</span><h1>ایده، زمین یا پروژه‌ای دارید؟<br>درباره‌اش صحبت کنیم.</h1><p>اطلاعات اولیه را ارسال کنید؛ تیم آریا سازه برای بررسی و هماهنگی جلسه با شما تماس می‌گیرد.</p></div></section>
<section class="section"><div class="lux-container contact-lux-grid"><aside class="contact-info"><img src="{{ asset('assets/construction/admin-banner.svg') }}" alt="دفتر آریا سازه" width="1200" height="420" loading="lazy"><div>@if($siteSettings['phone'] ?? null)<p><small>تلفن</small><a href="tel:{{ preg_replace('/\s+/', '', $siteSettings['phone']) }}">{{ $siteSettings['phone'] }}</a></p>@endif @if($siteSettings['email'] ?? null)<p><small>ایمیل</small><a href="mailto:{{ $siteSettings['email'] }}">{{ $siteSettings['email'] }}</a></p>@endif @if($siteSettings['address'] ?? null)<p><small>آدرس</small><span>{{ $siteSettings['address'] }}</span></p>@endif</div></aside><div class="contact-form-card">@if(session('success'))<div class="alert success">{{ session('success') }}</div>@endif @if($errors->any())<div class="alert error">لطفاً اطلاعات فرم را بررسی کنید.</div>@endif<form class="contact-form" method="POST" action="{{ route('contact.store') }}">@csrf<div class="honeypot" aria-hidden="true"><label>Website<input type="text" name="website" tabindex="-1" autocomplete="off"></label></div><div class="form-row"><label>نام و نام خانوادگی<input name="name" value="{{ old('name') }}" required autocomplete="name"></label><label>ایمیل<input type="email" name="email" value="{{ old('email') }}" required autocomplete="email"></label></div><div class="form-row"><label>شماره تماس<input name="phone" value="{{ old('phone') }}" autocomplete="tel"></label><label>شرکت / مجموعه<input name="company" value="{{ old('company') }}" autocomplete="organization"></label></div><label>موضوع<input name="subject" value="{{ old('subject') }}" placeholder="مثلاً ساخت ویلا در لواسان"></label><label>درباره پروژه توضیح دهید<textarea name="message" rows="7" required placeholder="موقعیت، متراژ، نوع پروژه و اطلاعات مفید ...">{{ old('message') }}</textarea></label><button class="gold-btn" type="submit">ارسال درخواست ←</button></form></div></div></section>
@endsection
