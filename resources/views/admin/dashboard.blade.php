@extends('layouts.admin')
@section('title','داشبورد')
@section('content')
<div class="admin-hero"><section class="admin-welcome"><span class="gold-kicker">پنل مدیریت آریا سازه</span><h1>سلام {{ auth()->user()->name }} 👋</h1><p>امروز فرصت دیگری برای ساختن یک پروژه بهتر است.</p></section><div class="admin-banner"><img src="{{ asset('assets/construction/admin-banner.svg') }}" alt="" width="1200" height="420"></div></div>
<div class="stat-grid">@foreach($stats as $label=>$value)<article class="stat-card"><span>{{ $label }}</span><strong>{{ number_format($value) }}</strong><small>آمار فعلی</small></article>@endforeach</div>
<div class="admin-grid"><section class="admin-card"><div class="admin-card-head"><div><h3>آخرین پیام‌ها و درخواست‌ها</h3><p>سرنخ‌های جدید ثبت‌شده از فرم تماس سایت.</p></div><a class="text-arrow" href="{{ route('admin.messages.index') }}">مشاهده همه ←</a></div><div class="admin-table-wrap"><table class="admin-table"><thead><tr><th>نام</th><th>موضوع</th><th>وضعیت</th><th>تاریخ</th><th></th></tr></thead><tbody>@forelse($messages as $message)<tr><td><strong>{{ $message->name }}</strong><small>{{ $message->email }}</small></td><td>{{ $message->subject ?: 'درخواست عمومی' }}</td><td><span @class(['status-pill','is-live'=>$message->is_read,'is-draft'=>!$message->is_read])>{{ $message->is_read ? 'بررسی شده' : 'جدید' }}</span></td><td>{{ $message->created_at->format('Y/m/d') }}</td><td><a class="text-arrow" href="{{ route('admin.messages.show',$message) }}">باز کردن ←</a></td></tr>@empty<tr><td colspan="5">هنوز پیامی ثبت نشده است.</td></tr>@endforelse</tbody></table></div></section><aside class="admin-card"><div class="admin-card-head"><div><h3>دسترسی سریع</h3><p>عملیات پرتکرار مدیریت.</p></div></div><div class="quick-grid"><a class="quick-action" href="{{ route('admin.cms.create','projects') }}"><strong>＋ پروژه جدید</strong><small>ثبت برج، ویلا یا خانه</small></a><a class="quick-action" href="{{ route('admin.cms.create','posts') }}"><strong>▤ مقاله جدید</strong><small>انتشار در وبلاگ</small></a><a class="quick-action" href="{{ route('admin.cms.index','services') }}"><strong>◇ مدیریت خدمات</strong><small>ویرایش خدمات شرکت</small></a><a class="quick-action" href="{{ route('admin.settings.edit') }}"><strong>⚙ تنظیمات</strong><small>SEO و اطلاعات سایت</small></a></div></aside></div>
<section class="admin-card" style="margin-top:14px"><div class="admin-card-head"><div><h3>آخرین پروژه‌ها</h3><p>وضعیت آخرین پروژه‌های ثبت‌شده.</p></div><a class="text-arrow" href="{{ route('admin.cms.index','projects') }}">مدیریت پروژه‌ها ←</a></div><div class="recent-projects">@foreach($recentProjects as $project)<a class="recent-project" href="{{ route('admin.cms.edit',['projects',$project->id]) }}"><img src="{{ \App\Support\Media::url($project->image,'assets/construction/project-villa-01.svg') }}" alt="" width="100" height="80"><div><strong>{{ $project->title }}</strong><small>{{ $project->location }}</small></div><span class="status-pill is-draft">{{ $project->status }}</span></a>@endforeach</div></section>
@endsection
