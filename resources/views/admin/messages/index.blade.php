@extends('layouts.admin')
@section('title','پیام‌های تماس')
@section('content')
<div class="admin-intro"><div><h2>پیام‌ها و درخواست‌های مشتریان</h2><p>درخواست‌های مشاوره و تماس ارسال‌شده از وب‌سایت.</p></div></div><section class="admin-card"><div class="admin-table-wrap"><table class="admin-table"><thead><tr><th>نام</th><th>موضوع</th><th>ایمیل / تلفن</th><th>وضعیت</th><th>تاریخ</th><th></th></tr></thead><tbody>@forelse($messages as $message)<tr><td><strong>{{ $message->name }}</strong><small>{{ $message->company }}</small></td><td>{{ $message->subject ?: 'درخواست عمومی' }}</td><td>{{ $message->email }}<small>{{ $message->phone }}</small></td><td><span @class(['status-pill','is-live'=>$message->is_read,'is-draft'=>!$message->is_read])>{{ $message->is_read ? 'بررسی شده' : 'جدید' }}</span></td><td>{{ $message->created_at->format('Y/m/d H:i') }}</td><td><a class="text-arrow" href="{{ route('admin.messages.show',$message) }}">مشاهده ←</a></td></tr>@empty<tr><td colspan="6">هنوز پیامی وجود ندارد.</td></tr>@endforelse</tbody></table></div><div class="pagination-wrap">{{ $messages->links() }}</div></section>
@endsection
